📋 科学学习方法（边做边查，不卡壳）
第 1 步：跑起来（不要读代码）
按 README 安装依赖、申请 API Key、启动 Streamlit。目标只有一个：看到一个能提问的界面。遇到报错就搜报错信息，解决完继续。

第 2 步：带着“数据流”读代码
启动成功后，按 src/ 的目录顺序读：document_processing → embeddings → vector_database → generation。每读一个文件，问自己：它在数据流中的哪一环？输入什么？输出什么？ 不理解的函数名立刻查，但不要追太深。

第 3 步：改一个小东西，观察变化
比如：

把 chunk_size 改小，重新上传文档，看回答质量有没有变化。

在 generation/rag.py 的 Prompt 里加一句话（比如“回答时尽量简洁”），看输出变化。
这一步的目的是建立“我改的代码 → 系统行为变化”的因果感。

第 4 步：用你的八股资料反向查漏
拿着小林 coding 的 Agent 八股，看哪些问题能在这个项目里找到答案。比如看到“RAG 的 chunking 策略怎么选”，就回到项目的 document_processing 里看它怎么做的，然后思考“如果 chunk_overlap 设成 0 会怎样”。





🟡 第二层：用到再查的知识点（不要提前啃）
以下知识点，遇到对应代码再去查，查完立刻回到项目里验证。

1. 文档解析（src/document_processing/）

用到 PyMuPDF 时，查“PyMuPDF 提取 PDF 文本和页码”即可。

理解 chunking（切块） 的概念：为什么要把长文档切成小块？chunk_size 和 chunk_overlap 分别控制什么。

这个项目的切块还带有元数据（页码、时间戳），因为要支持引用。

2. 向量数据库（src/vector_database/）

用到 Milvus 时，只需理解它的角色：存向量、做相似度搜索。

遇到“为什么选 Milvus 而不是 FAISS/Chroma”这种面试问题时，知道 Milvus 适合大规模、分布式场景即可。

3. Embedding 模型（src/embeddings/）

项目用的 FastEmbed 是一个轻量级 Embedding 库。

不需要背模型排行榜，但要知道 Embedding 模型的选择会影响检索质量。

4. Zep 记忆层（src/memory/）—— 这是项目的差异化亮点

Zep 是一个时序知识图谱记忆系统，不是简单的向量记忆。

它把对话中的实体和关系抽取出来，存成随时间变化的知识图谱，解决“聊了很久之后模型忘了之前说过什么”的问题。

面试加分点：能说出 Zep 比简单的“把历史对话拼进 Prompt”或“向量化历史”好在哪里。

5. 播客生成（src/podcast/）

这是项目的一个“Studio 功能”，用 CrewAI 编排多智能体协作：一个 agent 写脚本，一个负责别的角色。

用到 TTS（文本转语音） 时再查 Kokoro 或 ElevenLabs 的用法即可。

真正需要补的“技术栈”（按优先级）
第一优先：Streamlit 基础
这是项目的 UI 层。学会 st.file_uploader()、st.chat_input()、st.write() 这几个就够了，半天可以上手。

第二优先：CrewAI 的最小用法
项目用 CrewAI 封装 LLM 调用和播客脚本生成。你只需要理解：Agent（角色）+ Task（任务）+ Crew（编排）这个基本模型，不需要深入其多智能体协作机制。

第三优先：API Key 管理
项目依赖多个外部服务（AssemblyAI、Zep、OpenAI）。你需要会：申请 Key、放进 .env、用 os.getenv() 读取。这是纯工程习惯，5 分钟学会。


🟢 第三层：面试包装时需要补的（做完项目后集中准备）
那些写到简历的项目经历亮点，一定要提前准备好。这个准备过程需要看JD,借助AI挖掘，甚至还可以包装一下。这样就可以引导面试官向你擅长的地方提问。


技术栈清单：

职责	技术选型	你需要学到什么程度
文档解析	PyMuPDF	会调 page.get_text() 即可
嵌入模型	fastembed	知道它是把文本转成向量的工具
向量库	Milvus Lite	会建集合、插入、检索，单文件模式，零配置
音频转录	AssemblyAI	调 API
网页抓取	Firecrawl	调 API
LLM 调用	CrewAI 的 LLM 封装	会传 prompt、收结果
记忆层	Zep Cloud	调 API，知道它是“有时序的知识图谱”
TTS	Kokoro	调库
Web UI	Streamlit	会写按钮、上传、显示
YouTube 下载	yt-dlp + FFmpeg	调命令

就是从零基础开始，那这个时候就是先做一个简历，针对简历上的技术栈之后找一个有成熟框架的后端项目，然后从上往下逐步解构，分不同模块进行自我假设提问




按"面试官最容易理解、最容易产生追问兴趣"
  的程度排序

  第1名：AI Podcast
  Generation（AI播客生成）
  - 排第一的原因：最直观、最吸引人的功能。
    即使是非技术背景的面试官也能立刻理解"
    把文档变成播客"的价值，不需要懂底层技
    术。天然容易引发对话。

  第2名：Citation-First
  Approach（引用优先方法）
  - 排第二的原因：解决了一个广为人知的真实
    问题（AI幻觉/准确性问题）。用过ChatGPT
    或NotebookLM的面试官会立即明白其价值。
    "页码和引用"这个细节让它变得具体可感。

  第3名：Multi-Format
  Support（多格式支持）
  - 排第三的原因：用简单易懂的方式展示技术
    广度。每个人都知道PDF、视频、音频文件
    。展示了实用性，不需要深厚技术知识就能
    理解价值。

  第4名：Memory-Powered（记忆驱动 -
  时序知识图谱）
  - 排第四的原因：好处很清楚（"记住上下文"
    ），但"时序知识图谱"听起来很吓人，可能
    引发你还没准备好的技术问题。如果面试官
    是技术背景，可能会适得其反。

  第5名：Efficient Retrieval（高效检索）
  - 排最后的原因：太模糊、太技术化。"chunk
    s"（数据块）和"citation
    metadata"（引用元数据）需要了解RAG知识
    才能理解价值。除非面试官本身就很技术，
    否则不会引发自然对话。

  排名前3的亮点 - 面试官最可能追问的问题

  1. AI Podcast Generation（AI播客生成）

  - 问题1："它怎么生成多个说话人的？你能控
    制他们的声音或对话风格吗？"
  - 问题2："这个功能的使用场景是什么？什么
    时候用户会想要播客而不是直接读文档？"
  - 问题3："生成一个播客需要多长时间？比如
    一个50页的文档？"

  2. Citation-First
  Approach（引用优先方法）

  - 问题1："你怎么确保引用是准确的？如果AI
    误解了源材料怎么办？"
  - 问题2："能给我展示一个例子吗？它和普通
    的ChatGPT回答有什么不同？"
  - 问题3："如果文档没有页码怎么办？比如网
    页内容？"

  3. Multi-Format Support（多格式支持）

  - 问题1："你怎么从音频或视频中提取文本？
    用的是语音转文字API吗？"
  - 问题2："最难处理的格式是什么？为什么？
    "
  - 问题3："它能同时处理多个文件吗？比如把
    PDF和YouTube视频结合起来？"

  ---

  我的真诚建议：既然你是编程新手，打算面试
  后再学这些概念，建议只写前3个。把第4和第
  5完全去掉——它们技术性太强，可能会遇到你
  答不上来的问题，这样反而会损害可信度，比
  不写它们的负面影响更大。

可以。你现场演示时不要一上来讲代码，建议按照“**用户痛点 → 产品演示 → 技术原理 → 工程亮点**”来讲。

下面这套话术可以直接用于面试。

---

## 一、30 秒项目介绍

> 这是一个 NotebookLM 风格的知识库问答 Demo。用户可以上传 PDF、Markdown、TXT、音频、YouTube 视频或网页内容，系统会自动解析内容、切分文本、生成向量并存入本地向量数据库。之后用户可以基于自己的资料进行带来源引用的问答，还可以把资料生成播客脚本和音频。
>
> 这个项目的核心是一个 RAG 流程：先从用户资料中检索相关内容，再把检索结果交给大模型生成回答，从而让回答尽量基于用户上传的资料，而不是完全依赖模型记忆。

---

## 二、2 分钟技术介绍

这个项目主要由五层组成：

### 1. 交互层

使用 Streamlit 搭建界面，主要包含三个页面：

- Add Sources：上传和处理资料；
- Chat：基于资料进行问答；
- Studio：生成播客脚本和音频。

入口文件是：

- [app.py]()

---

### 2. 文档处理层

用户上传文件后，系统会根据文件类型进行处理：

- PDF：提取文本和页码；
- TXT/Markdown：读取文本；
- 音频：调用 AssemblyAI 转录；
- YouTube：提取视频内容并转录；
- 网页：调用 Firecrawl 抓取内容；
- 粘贴文本：直接作为资料处理。

文档会被切成多个 Chunk，每个 Chunk 会保留：

- 内容；
- 文件名；
- 来源类型；
- 页码；
- Chunk 编号；
- 字符起止位置。

这样后续回答时可以知道信息来自哪里。

---

### 3. 向量化层

每个文本 Chunk 会通过 FastEmbed 生成向量：

```text
文本 → Embedding 向量
```

当前使用的是：

```text
sentence-transformers/all-MiniLM-L6-v2
```

这样系统可以根据语义相似度搜索内容，而不是只做关键词匹配。

相关代码：

- [embedding_generator.py]()

---

### 4. 向量数据库层

项目使用 Milvus Lite 作为本地向量数据库。

每个 Chunk 会保存：

- 原始文本；
- 向量；
- 来源文件；
- 页码；
- 文档类型；
- Chunk 元数据。

相关代码：

- [milvus_vector_db.py]()

当前是本地 Milvus Lite，不需要单独部署远程数据库，适合 Demo 和本地开发。

---

### 5. RAG 和大模型层

用户在 Chat 中提问时，流程是：

```text
用户问题
   ↓
生成问题向量
   ↓
Milvus 相似度检索
   ↓
取得相关文档片段
   ↓
拼接上下文和引用信息
   ↓
发送给大模型
   ↓
返回带来源的回答
```

核心代码：

- [rag.py]()

目前大模型通过 OpenRouter 的 OpenAI 兼容接口调用：

```env
OPENAI_BASE_URL=https://openrouter.ai/api/v1
```

---

## 三、现场演示流程

建议准备一个小型 Markdown 或 PDF 文件，不要现场临时找复杂资料。

例如准备一份 2～3 页的技术文档，内容包括：

- 项目背景；
- 三个关键概念；
- 一个具体例子；
- 一个容易混淆的地方。

### 第一步：打开项目

```text
http://localhost:8501
```

介绍：

> 这是应用首页，左侧是当前已经加载的资料，主区域有资料上传、聊天和播客三个功能。

---

### 第二步：上传资料

点击：

```text
Add Sources → Upload sources
```

上传 PDF 或 Markdown，然后点击：

```text
Process Files
```

可以这样讲：

> 这里不是简单把文件传给大模型，而是先进行本地解析和切分，然后生成 embedding，最后写入 Milvus Lite。这样后续可以只检索和问题相关的内容，不需要每次把整个文件都发送给模型。

---

### 第三步：演示 Chat

切换到 Chat，提问一个资料中明确存在的问题，例如：

```text
这份资料主要解决了什么问题？
```

然后继续问：

```text
请比较文档中提到的两种方法，它们分别适合什么场景？
```

再问一个资料里没有的问题：

```text
这份资料是否讨论了 Transformer？
```

你可以这样解释：

> 这里的回答不是单纯调用大模型，而是先从 Milvus 中检索相关片段，再把片段和问题一起交给模型。引用信息可以帮助用户判断回答来自哪份资料。

---

### 第四步：演示来源引用

如果回答中出现 `[1]`、`[2]` 等引用，可以鼠标悬停。

介绍：

> 每个引用都对应检索到的 Chunk，系统还会保存来源文件和页码信息。这样可以降低模型胡编的风险，也方便用户回到原文核对。

---

### 第五步：演示 Studio

切换到 Studio，选择刚才上传的资料，然后点击：

```text
Generate Podcast
```

可以这样讲：

> Studio 会先从向量数据库中按来源筛选内容，然后调用大模型生成两个 Speaker 的对话脚本。音频合成部分使用 Kokoro TTS，并且采用惰性初始化，只有用户真正生成播客时才加载，避免影响首页启动速度。

这里要注意：免费模型可能会遇到限流。如果现场演示不稳定，建议提前生成一次，或者准备一个录屏/截图作为备用。

---

## 四、项目亮点怎么讲

面试官通常比较关心这些点。

### 亮点 1：不是简单调用大模型

可以说：

> 这个项目的重点不是调用一次 Chat API，而是完整实现了从非结构化资料到可检索知识库，再到带引用问答的闭环。

---

### 亮点 2：保留元数据

可以说：

> 在切分文本时，我没有只保存纯文本，而是同时保存来源文件、页码、Chunk 编号和字符位置。这让后续的引用展示和问题定位成为可能。

---

### 亮点 3：本地向量数据库

可以说：

> 选择 Milvus Lite 是为了降低 Demo 部署成本。它可以在本地运行，不需要额外启动独立的向量数据库服务，但接口和正式 Milvus 基本一致，后续可以平滑迁移到远程 Milvus。

---

### 亮点 4：处理启动速度问题

你之前遇到的启动慢也可以包装成工程经验：

> 项目早期启动时会同步初始化 embedding 模型、Milvus、Zep 和 TTS。后来我发现 TTS 会下载额外模型，导致首页长时间处于 loading 状态。因此我把 TTS 改成惰性初始化，只有用户生成播客时才加载。同时为模型下载配置了 Hugging Face 镜像和本地缓存。

对应修改在：

- `app.py`

这是一个很好的“遇到问题—定位原因—进行优化”的项目故事。

---

### 亮点 5：异常处理

可以说：

> 对 API 超时、空响应、认证失败和免费模型限流，我增加了明确的错误提示和有限超时，避免前端一直显示加载状态。

---

## 五、面试官问“为什么使用 RAG”时

可以这样回答：

> 如果直接把所有文档内容放进 Prompt，会有三个问题：上下文长度受限、成本高、相关信息不容易控制。RAG 会先把问题转换为向量，然后只检索最相关的几个片段，再交给模型处理。这样可以降低 Token 消耗，同时提高回答和用户资料的相关性。

---

## 六、面试官问“RAG 有什么问题”时

可以这样回答：

> RAG 不代表一定正确，主要风险包括切分不合理、检索不到正确片段、召回了相似但不相关的内容，以及模型对检索内容理解错误。这个项目通过保留元数据、限制上下文长度、展示来源引用来提高可解释性。后续还可以增加 reranker、混合检索和评估集。

---

## 七、面试官问“下一步怎么优化”

可以回答：

1. 增加混合检索：

```text
向量检索 + BM25 关键词检索
```

2. 增加 Reranker，对召回结果重新排序；
3. 增加文档级权限控制；
4. 增加问答评估集，测量：

```text
Recall@K
回答相关性
引用准确率
幻觉率
```

5. 对 embedding 和模型进行缓存；
6. 使用任务队列处理音频转录和播客生成；
7. 将 Milvus Lite 迁移到正式 Milvus 或云数据库；
8. 使用结构化输出确保播客脚本稳定生成；
9. 增加 API Key 脱敏、轮换和环境变量校验。

---

## 八、现场演示时最重要的注意事项

### 不要展示 API Key

现场不要打开 `.env` 文件，也不要把 Key 粘贴到聊天或截图中。

### 提前准备资料

准备一个小文件，不要现场上传几十 MB 的 PDF。

### 提前运行一次

提前启动：

```powershell
cd E:\CSUcode\vibeCodingProjects\ai-engineering-hub\notebook-lm-clone
uv run streamlit run app.py
```

然后确认：

```text
http://localhost:8501
```

可以正常打开。

### 准备备用问答

如果 OpenRouter 免费模型限流，Chat 或 Studio 可能失败。可以准备：

- 一张成功运行截图；
- 一段预生成的播客音频；
- 一份简短的演示视频；
- 一个不依赖模型的本地演示流程。

---

## 九、完整的现场讲稿

你可以直接这样说：

> 我演示的是一个 NotebookLM 风格的本地知识库问答系统。首先我上传一份资料，系统会对资料进行解析和切分，然后使用 embedding 模型把每个文本片段转换成向量，并保存到 Milvus Lite 中。
>
> 接下来我在 Chat 中提问。系统不会直接把问题发送给大模型，而是先把问题向量化，从 Milvus 中检索最相关的文本片段，再将这些片段和问题一起发送给大模型生成回答。因此回答会尽量基于用户上传的资料，并且可以展示来源引用。
>
> 在 Studio 中，系统会针对选中的资料生成两个 Speaker 的播客脚本，随后可以通过 TTS 合成音频。
>
> 这个项目中我比较重视工程稳定性。例如最开始 TTS 模型在应用启动时加载，导致页面长时间 loading。后来我把它改成惰性初始化，并给 API 请求增加了超时和错误提示，使首页可以快速启动。
>
> 这个 Demo 的核心价值是把文档处理、向量检索、大模型生成、引用溯源和多模态输出串成了一个完整闭环。

---

目前项目的主线可以概括成一句话：

> **这是一个支持文档理解、基于 RAG 的引用问答，以及自动播客生成的 NotebookLM 风格 AI 应用。**